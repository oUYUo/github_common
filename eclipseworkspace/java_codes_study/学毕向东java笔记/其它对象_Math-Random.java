/*
import java.lang;
Math 静态数学类方法：
	1，取大于指定数据的最小整数：static double ceil(double a)
	   调用方式：Math.ceil( 16.34);  结果：17

	2，取小于指定数据的最大整数：static double floor(double a)
	   调用方式：Math.floor( 12.34);  结果12

	3，四舍五入：static long round(double a);
	   调用方式：Math.round( 12.54);  结果13

	4，取随机0至小于1的小数：static double random()
	   调用方式：double d = Math.random();

--------------------------------------------------------------------------------

import java.util;
Random 随机数生成器类方法：
	1，构造方法，创建新随机数生成器：Random()
	   调用方式：Random r = new Random();

	2，随机取0至小于指定值：int nextInt(int n)
	   调用方式：对象.nextInt( 10);      //随机取0到10（不包括10）之间数。

*/