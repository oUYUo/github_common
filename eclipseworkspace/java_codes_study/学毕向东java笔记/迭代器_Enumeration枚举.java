/*
java.util包
关系：Vector类，elements()方法 创建 Enumeration<E> 对象。

接口 Enumeration<E> 方法：
	1，判断枚举是否有元素：boolean hasMoreElements();
	   相似：Iterator 迭代器 hasNext()方法。
	   调用方式：对象.hasMoreElements();

	2，返回列表中的下一个元素：E nextElement();
	   相似：Iterator 迭代器 next()方法。
	   调用方式：对象.nextElement();
*/